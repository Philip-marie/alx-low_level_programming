#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (success)
 */
int main(void)
{
	char c;
	int d;

	c = 'a';
	d = 'o';
	while
		(d < 10) {
			putchar(d + 'o');
			d++;
		}
	while
		(c <= 'f') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
