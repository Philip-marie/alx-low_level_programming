#include "main.h"
/**
 * _isdigit - Function that checls for a digit (0 throigh 9)
 * @c: integer o be checked
 * Return: 1 if c is a digit, otherwise 0
 */
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
