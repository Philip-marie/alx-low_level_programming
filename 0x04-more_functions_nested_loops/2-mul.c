#include "main.h"
/**
 * mul - Function checking for a digit (0 through)
 * @a: For variable
 * @b: For variable
 * Return: Always (0)
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
